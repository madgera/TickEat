name: tickeat
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  
  # Database locale
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.0+4
  path: ^1.9.0
  
  # State management
  provider: ^6.1.1
  
  # UI Componentss
  flutter_barcode_scanner: ^2.0.0
  
  # Stampa termica
  esc_pos_printer: ^4.1.0
  esc_pos_utils: ^1.1.0
  
  # Bluetooth - BLE e SPP
  flutter_blue_plus: ^1.35.5
  flutter_bluetooth_serial: ^0.4.0
  
  # Network discovery per stampanti Wi-Fi/LAN
  network_info_plus: ^5.0.3
  
  # File handling e export
  pdf: ^3.8.4
  open_file: ^3.5.10
  path_provider: ^2.1.2
  permission_handler: ^11.3.0
  
  # Networking per versione Super
  http: ^1.2.0
  web_socket_channel: ^2.4.0
  
  # Utilità
  intl: ^0.19.0
  uuid: ^4.3.3
  shared_preferences: ^2.2.2

  # Crittografia per conformità fiscale
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
